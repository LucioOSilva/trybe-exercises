const stringToUpper = (phrase) => phrase.toUpperCase();
const stringFisrtLetter = (phrase) => phrase[0];
const stringJoin = (phrase1, phrase2) => `${phrase1}${phrase2}`;

module.exports = { stringToUpper, stringFisrtLetter, stringJoin };
